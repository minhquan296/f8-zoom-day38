import styles from "../../ModalDemo.module.scss";

const ButtonCloseModal = () => {
	return (
		<button onClick={() => {}} className={styles.modalActionClose}>
			close
		</button>
	);
};
export default ButtonCloseModal;
